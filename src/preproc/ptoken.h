/* the preprocessor and the translator are well integrated by now, ltoken.h is
 * derived from the rtt grammar
 */
#include "../rtt/ltoken.h"

/* Tokens exclusive to the preprocessor (IDs > 1000) are defined in preproc.h
 */
